# Copy to .env and adjust. Only RESOLVEAI_LLM_MODE matters for the mock demo.

RESOLVEAI_LLM_MODE=mock            # mock | anthropic

# Required only when RESOLVEAI_LLM_MODE=anthropic
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-sonnet-5

# Optional path overrides
# RESOLVEAI_DATA_DIR=./data
# RESOLVEAI_DB_PATH=./resolveai_memory.db
